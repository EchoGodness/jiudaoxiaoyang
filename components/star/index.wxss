.container {
      display: inline-flex;
      flex-direction: row;
      padding: 10rpx;
      /* 指定宽度，否则会出现移动, 数字显示使用 k */
      /* width: 120rpx; */
}

.container image {
      width: 32rpx;
      height: 28rpx;
}

.container text {
      position: relative;
      left: 6rpx;
      bottom: 10rpx;
      color: "#bbb";
      font-size: 24rpx;
      /* 取消字体上下间距 */
      line-height: 24rps;
      /* 取消字体上下间距 */
}

.container .icon {
      width: 34rpx;
      height: 34rpx;
      margin-left:40rpx;
}
